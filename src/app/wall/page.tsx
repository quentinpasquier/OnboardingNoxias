import { WallView } from "@/components/WallView";

export const metadata = {
  title: "Mur d'avancement · Noxias",
  robots: { index: false, follow: false },
};

export default function WallPage() {
  return <WallView />;
}
